var config = {
    database: 'test', // database to user
    username: 'www', // username
    password: 'www', // pw
    host: 'localhost', // hostname
    port: 3306 // port ，MySQL default 3306
};

module.exports = config;